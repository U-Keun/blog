---
{"sticker":"lucide//code-2","tags":["Code","OS","Rust"],"dg-publish":true,"permalink":"/areas/rust-mini-os/src/mem/paging/sv32-rs/","dgPassFrontmatter":true,"dg-note-properties":{"sticker":"lucide//code-2","tags":["Code","OS","Rust"]}}
---

RISC-V의 Sv32(2-level) 페이지 테이블을 구성하고, `satp`에 루트 페이지 테이블을 설치하여 페이징을 초기화하는 루틴을 구현한 것이다.

```rust
impl PAddr {
    #[inline] pub const fn ppn(self) -> u32 { (self.0 >> 12) as u32 }
}

impl VAddr {
    #[inline] pub const fn vpn1(self) -> usize { (self.0 >> 22) & 0x3ff }
    #[inline] pub const fn vpn0(self) -> usize { (self.0 >> 12) & 0x3ff }
    #[inline] pub const fn page_offset(self) -> usize { self.0 & (PAGE_SIZE - 1) }
}
```
기존에 작성해두었던 [[Areas/Rust_mini_OS/src/mem/addr.rs\|물리/가상 주소 구조체]](`PAddr`, `VAddr`)에 대해, Sv32 페이지 테이블 구현을 위해 필요한 보조 메서드를 몇 개 추가해두었다.

```rust
pub const SATP_SV32: usize = 1 << 31;

#[inline]
pub const fn satp_from_root(root_paddr: PAddr) -> usize {
    SATP_SV32 | (root_paddr.0 / PAGE_SIZE)
}
```
